VistaIFS.com – Static Site (v2 with rotating hero video)
========================================================

What's new
- The hero visual is now the rotating Vista logo (autoplay, muted, loop, playsinline).
- Circular frame with soft border and drop shadow.

Deploy
1) In Amplify Hosting, upload this new zip as a new version (Deploy without Git).
2) No DNS changes required if the domain is already attached.

Assets
- /assets/Vista_rotate_anim.mp4 is preloaded for snappy hero playback.